#include <iostream>
using namespace std;

int main() {
    int N;
    std::cin >> N;

    while (N<=100) {
        if (N>=90)
            std::cout << "A ";
        if (N>=90)
            std::cout << "A ";
        if (N>=90)
            std::cout << "A ";
        if (N>=90)
            std::cout << "A ";
    }
    return 0;
}