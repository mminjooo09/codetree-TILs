#include <iostream>
using namespace std;

int main() {
    int A, B, C, D, E;
    std::cin >> A >> B >> C >> D >> E;

    std::cout << (A>B) << endl;
    std::cout << (A>C) << endl;
    std::cout << (A>D) << endl;
    std::cout << (A>E) << endl;
    
    return 0;
}